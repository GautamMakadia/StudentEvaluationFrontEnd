import { useEffect } from "react"
import { useNavigate } from "react-router"
import { useAuth } from "~/context/authContext"

export default function Index() {
   
    return <h1>Student Evaluation</h1>
}